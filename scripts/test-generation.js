/**
 * Test script for blog generation system
 * Uses mock data to test functionality without API calls
 */

const fs = require('fs-extra');
const path = require('path');

class BlogGeneratorTest {
  constructor() {
    this.baseDir = process.cwd();
    this.articlesDir = path.join(this.baseDir, 'articles');
    this.imagesDir = path.join(this.baseDir, 'assets', 'images', 'articles');
  }

  async runTests() {
    console.log('🧪 Iniciando pruebas del sistema de generación de blog...\n');

    try {
      // Test 1: Directory creation
      await this.testDirectoryCreation();
      
      // Test 2: Mock article generation
      await this.testMockArticleGeneration();
      
      // Test 3: HTML file creation
      await this.testHtmlFileCreation();
      
      // Test 4: Index update
      await this.testIndexUpdate();
      
      console.log('\n✅ Todas las pruebas pasaron exitosamente!');
      console.log('\n📋 Próximos pasos:');
      console.log('1. Configurar OPENAI_API_KEY en GitHub Secrets');
      console.log('2. Configurar UNSPLASH_ACCESS_KEY en GitHub Secrets (opcional)');
      console.log('3. El sistema generará artículos automáticamente los lunes y jueves');
      console.log('4. También puedes generar manualmente con: npm run generate-blog');
      
      return true;
      
    } catch (error) {
      console.error('\n❌ Error en las pruebas:', error);
      return false;
    }
  }

  async testDirectoryCreation() {
    console.log('🔧 Test 1: Creación de directorios...');
    
    await fs.ensureDir(this.articlesDir);
    await fs.ensureDir(this.imagesDir);
    
    const articlesExists = await fs.pathExists(this.articlesDir);
    const imagesExists = await fs.pathExists(this.imagesDir);
    
    if (articlesExists && imagesExists) {
      console.log('   ✅ Directorios creados correctamente');
    } else {
      throw new Error('Falló la creación de directorios');
    }
  }

  async testMockArticleGeneration() {
    console.log('🔧 Test 2: Generación de artículo mock...');
    
    const mockArticle = {
      title: 'Cómo Mejorar tu Historial Crediticio en 2025',
      introduction: 'El historial crediticio es uno de los factores más importantes para acceder a financiamiento favorable. En este artículo te explicamos las mejores estrategias para mejorarlo.',
      sections: [
        {
          subtitle: 'Entendiendo tu Historial Crediticio',
          content: 'Tu historial crediticio es un registro de cómo has manejado tus deudas y pagos a lo largo del tiempo. Este incluye información sobre tarjetas de crédito, préstamos, y otros productos financieros.'
        },
        {
          subtitle: 'Estrategias para Mejorar tu Score',
          content: 'Existen varias estrategias comprobadas para mejorar tu puntuación crediticia: pagar a tiempo, mantener bajos saldos, y revisar regularmente tu reporte de crédito.'
        },
        {
          subtitle: 'Beneficios de un Buen Historial',
          content: 'Un buen historial crediticio te abre puertas a mejores condiciones de financiamiento, tasas más bajas, y productos financieros exclusivos como los créditos vía nómina.'
        }
      ],
      conclusion: 'Mejorar tu historial crediticio es una inversión a largo plazo que te beneficiará en todas tus decisiones financieras futuras. Con disciplina y las estrategias correctas, puedes lograr un perfil crediticio excelente.',
      keyPoints: [
        'Paga siempre a tiempo para mantener un historial limpio',
        'Mantén tus saldos de tarjetas por debajo del 30% del límite',
        'Revisa tu reporte de crédito al menos una vez al año'
      ],
      metaDescription: 'Aprende las mejores estrategias para mejorar tu historial crediticio en 2025. Consejos prácticos y efectivos de Financiera Incentiva.',
      date: '3 de julio de 2025',
      slug: 'como-mejorar-tu-historial-crediticio-2025'
    };

    if (mockArticle.title && mockArticle.sections.length > 0) {
      console.log('   ✅ Estructura de artículo válida');
    } else {
      throw new Error('Estructura de artículo inválida');
    }
  }

  async testHtmlFileCreation() {
    console.log('🔧 Test 3: Creación de archivo HTML...');
    
    const testHtml = `<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Article | Financiera Incentiva</title>
</head>
<body>
    <h1>Test Article</h1>
    <p>This is a test article generated by the automated system.</p>
</body>
</html>`;

    const testFilePath = path.join(this.baseDir, 'test-article.html');
    await fs.writeFile(testFilePath, testHtml, 'utf8');
    
    const fileExists = await fs.pathExists(testFilePath);
    if (fileExists) {
      // Clean up test file
      await fs.remove(testFilePath);
      console.log('   ✅ Creación de HTML exitosa');
    } else {
      throw new Error('Falló la creación del archivo HTML');
    }
  }

  async testIndexUpdate() {
    console.log('🔧 Test 4: Actualización de índice...');
    
    const indexPath = path.join(this.articlesDir, 'index.json');
    const testIndex = [
      {
        title: 'Test Article',
        slug: 'test-article',
        date: new Date().toISOString(),
        description: 'This is a test article',
        path: 'test-article.html',
        category: 'Test'
      }
    ];

    await fs.writeJson(indexPath, testIndex, { spaces: 2 });
    
    const indexExists = await fs.pathExists(indexPath);
    if (indexExists) {
      const savedIndex = await fs.readJson(indexPath);
      if (savedIndex.length === 1 && savedIndex[0].title === 'Test Article') {
        console.log('   ✅ Actualización de índice exitosa');
      } else {
        throw new Error('Contenido del índice incorrecto');
      }
    } else {
      throw new Error('Falló la creación del índice');
    }
  }
}

// Execute if run directly
if (require.main === module) {
  const tester = new BlogGeneratorTest();
  tester.runTests()
    .then(success => {
      process.exit(success ? 0 : 1);
    })
    .catch(error => {
      console.error('❌ Error fatal en pruebas:', error);
      process.exit(1);
    });
}

module.exports = BlogGeneratorTest; 